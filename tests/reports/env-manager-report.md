# EnvManager Report — Infrastructure Readiness

Generated by: EnvManager Agent (orchestrated)
Date: 2026-05-18

---

## INFRASTRUCTURE

| Service       | Image                                    | Port  | Healthcheck          | Status  |
|---------------|------------------------------------------|-------|----------------------|---------|
| postgres      | timescale/timescaledb:latest-pg16        | 5432  | pg_isready (5s/10r)  | DEFINED |
| redis         | redis:7-alpine                           | 6379  | redis-cli ping (5s)  | DEFINED |
| elasticsearch | elasticsearch:8.13.0                     | 9200  | cluster/health grep  | DEFINED |

All three infra services have healthchecks configured. ✓

---

## ENV VARS

### Critical (service will not start without these)

| Variable              | Current Value        | Risk                                    |
|-----------------------|---------------------|-----------------------------------------|
| JWT_SECRET            | placeholder string   | ⚠️ MUST be ≥64 chars random in prod    |
| JWT_REFRESH_SECRET    | placeholder string   | ⚠️ MUST be ≥64 chars random in prod    |
| TOKEN_ENCRYPTION_KEY  | placeholder string   | ⚠️ MUST be 32-byte hex (64 hex chars)  |
| DATABASE_URL          | localhost default    | OK for local dev                        |
| REDIS_URL             | localhost default    | OK for local dev                        |

### Required for billing flows
| Variable                    | Status                  |
|-----------------------------|-------------------------|
| STRIPE_SECRET_KEY           | sk_test_... placeholder |
| STRIPE_WEBHOOK_SECRET       | whsec_... placeholder   |
| STRIPE_PRICE_*              | 6× price_... placeholders |

### Required for AI features
| Variable          | Status                   |
|-------------------|--------------------------|
| ANTHROPIC_API_KEY | placeholder — grader/keywords falls back gracefully |

### Optional (feature-flagged)
| Variable          | Default | Effect if missing                      |
|-------------------|---------|----------------------------------------|
| RESEND_API_KEY    | none    | Email skipped, logged as warning       |
| BRIGHTDATA_*      | none    | Scraping disabled (ENABLE_SCRAPING=false) |
| POSTHOG_KEY       | none    | Analytics tracking disabled            |

---

## SCHEMA

**Expected tables: 16** (users, email_verifications, password_resets, refresh_tokens, api_keys, etsy_connections, shops, listings, keywords, keyword_lists, keyword_list_items, tracked_shops, shop_snapshots, listing_grades, notifications, usage_events)

All 16 tables confirmed in `packages/db/src/schema.ts`. ✓

**Critical indexes verified:**
- listings: shop, category, opportunity_score, last_scraped
- notifications: (user_id, read, created_at)
- usage_events: (user_id, event_type, created_at)
- tracked_shops: unique(user_id, etsy_shop_id)

---

## TIMESCALEDB

**Hypertables: 3**
- listing_metrics (partitioned by recorded_at, 7-day chunks)
- keyword_metrics (partitioned by recorded_at, 7-day chunks)
- shop_metrics (partitioned by recorded_at, 7-day chunks)

**Continuous aggregates: 2**
- listing_daily_agg (1-day bucket, refreshes hourly)
- listing_weekly_agg (7-day bucket, refreshes daily)

**Retention policy:** listing_metrics → 90 days ✓

---

## BLOCKERS

| Priority | Issue                                              | Action Required                        |
|----------|----------------------------------------------------|----------------------------------------|
| HIGH     | JWT_SECRET is placeholder — auth tests will fail   | Set ≥64-char secret in .env.test       |
| HIGH     | TOKEN_ENCRYPTION_KEY must be exactly 64 hex chars  | Generate: `openssl rand -hex 32`       |
| MEDIUM   | ES start_period=30s — CI must wait before tests    | Add health-wait step in CI workflow    |
| MEDIUM   | TimescaleDB migration not auto-run on compose up   | Add `pnpm db:push` step before E2E     |
| LOW      | NEXTAUTH_SECRET < 32 chars in placeholder          | Set ≥32 chars in .env.test             |

---

## READINESS

**Status: NEEDS_ACTION**

Required before running full test suite:
```bash
# 1. Generate test secrets
JWT_SECRET=$(openssl rand -hex 32)
JWT_REFRESH_SECRET=$(openssl rand -hex 32)
TOKEN_ENCRYPTION_KEY=$(openssl rand -hex 32)
NEXTAUTH_SECRET=$(openssl rand -hex 16)

# 2. Start infra
docker compose -f docker-compose.infra.yml up -d

# 3. Wait for ES (slow start)
sleep 35

# 4. Run migrations
pnpm db:push

# 5. Seed plan limits in Redis
redis-cli HSET plan:limits:free    research_searches 5   grades 2   keywords 10  competitors 0  exports 1
redis-cli HSET plan:limits:starter research_searches 50  grades 20  keywords 100 competitors 5  exports 10
redis-cli HSET plan:limits:pro     research_searches 500 grades 200 keywords -1  competitors 25 exports 50
redis-cli HSET plan:limits:agency  research_searches -1  grades -1  keywords -1  competitors 100 exports -1
```
